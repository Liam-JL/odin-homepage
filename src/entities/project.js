export class Project {
    constructor(title, description, screenshotPath, githubLink, siteLink) {
        this.title = title;
        this.description = description;
        this.screenshotPath = screenshotPath;
        this.githubLink = githubLink;
        this.siteLink = siteLink;
    }
}